export default function Home() {
  return (
    <main className="mx-auto flex min-h-[calc(100vh-4rem)] max-w-3xl flex-col items-start justify-center gap-4 px-6">
      <h1 className="text-4xl font-semibold">openvidstudio</h1>
      <p className="text-base text-[var(--foreground)]/70">
        Free, open-source video-generation add-on for any software project,
        driven entirely through MCP tools.
      </p>
      <p className="text-sm text-[var(--foreground)]/50">
        This site is scaffolding. Landing page content lands in a later task.
      </p>
    </main>
  );
}
