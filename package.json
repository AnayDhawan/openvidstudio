{
  "name": "openvidstudio",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "build": "pnpm --filter @openvidstudio/capture build && pnpm --filter @openvidstudio/mcp-server build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @openvidstudio/mcp-server test"
  }
}
